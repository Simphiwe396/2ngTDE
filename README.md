ClinchGlow — Full Project (Final)

Included in this ZIP:
- server.js
- public/ (frontend: index, checkout, success, cancel, admin)
- public/assets/ (logo.svg, product images)
- .env (contains PayFast sandbox credentials; MONGO_URI and ADMIN_KEY are intentionally left blank)

IMPORTANT:
- Do NOT commit .env to a public repo.
- After deploying, set MONGO_URI and ADMIN_KEY in your Render environment variables.

Run locally:
1. npm install
2. node server.js
3. Visit http://localhost:4000
